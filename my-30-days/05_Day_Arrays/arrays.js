//arrays 


const test = ['string0', 'string1' , 'string2', 'string3', 'string4','string5' 'string6']

let length =  test.length
let first =  test[0]

let find =  test.length / 2
let middle = test[find]
let lastindex = test.length -1
let last = test[lastindex]

const names = ['John', 23, 'Mateo', 89, 'Vicente', 43 ,'Greg', 12]
const companies = ['Facebook', 'Google', 'Microsoft', 'Apple', 'IBM', 'Oracle ','Amazon']
